-  C언어에서 문자를 메모리에 저장하는 방식에 대하여 설명하라(아스키 코드 참고)
	- 해당 아스키 코드에 대응되는 문자 값을 저장 ('A' == 65)
- 널문자의 용도를 설명하라.
	- 문자열을 char 배열에 저장할 때 문자열의 끝을 나타내기 위해
- 메모리에 저장된 문자열의 끝을 찾는 방법을 설명하라.
	- 배열에 저장된 문자열에서 널 문자 '\0'을 찾는 코드
- 문자열의 길이를 구하는 알고리즘을 설명하라.
	- 널 문자 '\0'을 찾을 때까지 for문을 실행하고 해당 배열의 인덱스 값을 증가시킴